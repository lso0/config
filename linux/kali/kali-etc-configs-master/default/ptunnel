# Configuration for TCP over ICMP tunnelling daemon: ptunnel
#
# /etc/default/ptunnel

# Set to "true" if the ptunnel daemon should run
run_daemon="false"

# Set password for securing the tunnel, must also be set by the client
#password="mysecretpassword"
